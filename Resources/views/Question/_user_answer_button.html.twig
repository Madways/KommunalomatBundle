{% if answer == "0" %}
    <a href="{{ path("MadwaysKommunalomatBundleQuestionAnswer", {'weight': question_weight})  }}" class="button small radius success" style="margin:0; padding: 0.5em 1em;">▴</a>
{% elseif answer == "1" %}
    <a href="{{ path("MadwaysKommunalomatBundleQuestionAnswer", {'weight': question_weight})  }}" class="button small radius" style="margin:0; padding: 0.5em 1em;">-</a>
{% elseif answer == "2" %}
    <a href="{{ path("MadwaysKommunalomatBundleQuestionAnswer", {'weight': question_weight})  }}" class="button small radius alert" style="margin:0; padding: 0.5em 1em;">▾</a>
{% endif %}
